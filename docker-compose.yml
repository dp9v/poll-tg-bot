services:
  bot:
    build: .
    restart: unless-stopped
    environment:
      - BOT_TOKEN=${BOT_TOKEN}
      - CHAT_ID=${CHAT_ID}
      - BEARER_TOKEN=${BEARER_TOKEN}
      - STORAGE_PATH=/data/tgbot.db
    volumes:
      - bot_data:/data

volumes:
  bot_data:
